document.getElementById('add-box-btn').addEventListener('click', function() {
    // Candidate will complete this function
    // Task: Add a new little box into the box-container
    
    // const boxContainer = document.getElementById('box-container');
    // const template = document.getElementById('box-template').content.cloneNode(true);
    // const newBox = template.querySelector('.little-box');
    // newBox.textContent = boxContainer.children.length + 1;
    // boxContainer.appendChild(newBox);
});
